#pragma once
#include "n.h"

RGB(id) {
  int i = 0;
  while ('a' <= b[i] && b[i] <= 'z')
    i++;
  *o = i > 0 ? i : -1;
}
RGC(plus, '+')
RGC(mul, '*')
RGC(lp, '(')
RGC(rp, ')')

RGC(a, 'a')
RGC(b, 'b')
RGB(eps) { *o = 0; }
