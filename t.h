#pragma once
#include "n.h"

RGB(i) {
  int i = 0;
  while ('a' <= b[i] && b[i] <= 'z')
    i++;
  *o = i > 0 ? i : -1;
}
RGC(p, '+')
RGC(m, '*')
RGC(l, '(')
RGC(r, ')')

RGC(a, 'a')
RGC(b, 'b')
RGC(c, 'c')
RGC(d, 'd')
RGB(eps) { *o = 0; }
